var Banner = function(core) {
    this.config = {
        width: 930,
        height: 180,
    }

    this.loadQueue = [
        // 'logo.png'
    ];

    this.init = function() {
        console.log('init()');
    }

    this.start = function() {
        console.log('start()');
    }
}
